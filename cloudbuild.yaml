steps:
  # Build the container image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/repo1/Service1:$COMMIT_SHA', '.']
  # Push the container image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/repo1/Service1:$COMMIT_SHA']
  # Deploy container image to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
    - 'run'
    - 'deploy'
    - 'Service1'
    - '--image'
    - 'gcr.io/$PROJECT_ID/repo1/Service1:$COMMIT_SHA'
    - '--region'
    - 'southamerica-west1'

# Indentation corrected here
images:
  - 'gcr.io/$PROJECT_ID/repo1/Service1:$COMMIT_SHA'